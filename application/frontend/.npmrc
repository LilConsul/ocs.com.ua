# Allow build scripts for specific packages
allow-builds.esbuild=true
allow-builds.sharp=true
allow-builds.better-sqlite3=true
allow-builds.core-js=true
allow-builds.@biomejs/biome=true

# Speed optimizations for slow servers
prefer-offline=true
fetch-retries=3
